# Madison Thantu

Personal website for <https://madisonthantu.github.io/about_mgt/>.

This repo is intentionally static. There is no Jekyll, bundler, npm, or build step required.

## Local Preview

Open `index.html` in a browser, or run:

```bash
python3 -m http.server 4000
```

Then visit <http://localhost:4000>.
